# Core / shared
smbus2>=0.4.3

# Servo pHAT (PCA9685) — requires Adafruit Blinka for board/busio
Adafruit-Blinka>=9.0.0
adafruit-circuitpython-servokit>=1.3.0

# GPIO / PWM (Pi 5 compatible, for fan control)
lgpio>=0.2.2.0
gpiozero>=2.0

# Audio capture & playback
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26

# Camera (Pi Camera Module 3 — libcamera backend)
# Pre-installed on Pi OS Bookworm; listed here for pip installs on other systems
picamera2>=0.3.12

# Testing
pytest>=8.0
