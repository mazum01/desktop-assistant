# Core / shared
smbus2>=0.4.3

# Servo pHAT (PCA9685) — requires Adafruit Blinka for board/busio
Adafruit-Blinka>=9.0.0
adafruit-circuitpython-servokit>=1.3.0

# GPIO / PWM (Pi 5 compatible, for fan control)
lgpio>=0.2.2.0
gpiozero>=2.0

# Audio capture & playback
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26

# Camera (Pi Camera Module 3 — libcamera backend)
# NOTE: picamera2 must be installed via apt (`python3-picamera2`) on the Pi,
# not pip. The venv must be created with `--system-site-packages` to see it.
# It is NOT listed here because pip-installing it on Pi 5 fails on the
# libcamera Python binding (no PyPI wheel exists).

# Web dashboard
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
psutil>=5.9.0

# Native C++ extensions (build with scripts/build_scrfd_decode.sh)
pybind11>=2.12.0

# Testing
pytest>=8.0
