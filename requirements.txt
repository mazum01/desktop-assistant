# Core / shared
smbus2>=0.4.3

# Servo pHAT (PCA9685-based — SparkFun Pi Servo pHAT)
adafruit-circuitpython-servokit>=1.3.0

# GPIO / PWM (Pi 5 compatible)
lgpio>=0.2.2.0
gpiozero>=2.0

# Audio capture & playback
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26

# Testing
pytest>=8.0
